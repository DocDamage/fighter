# Street Fighter Asset Lab

Godot 4.6 Mono project wrapper for the local fighting-game asset collection.

The existing sprite, audio, VFX, UI, and stage assets are intentionally left in place. Use `scenes/` for Godot scenes and `scripts/` for GDScript as the project grows.
